import { CallbackTemplateDirective } from './callback-template.directive';

describe('CallbackTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new CallbackTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
